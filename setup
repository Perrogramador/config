git clone https://github.com/Perrogramador/config && cd config && cp gitconfig ../.gitconfig && cp -r vim ../.vim && mkdir ../.vim/swp && mkdir ../.vim/undo

sudo apt install curl && curl -fLo ~/.vim/autoload/plug.vim --create-dirs \
    https://raw.githubusercontent.com/junegunn/vim-plug/master/plug.vim

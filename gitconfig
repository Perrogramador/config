[user]
    name = Perrogramador
    mail = rocher.c.g@gmail.com
[alias]
    a = add
    aa = add --all
    bl = blame
    br = branch
    bs = bisect
    c = commit
    ca = commit --amend
    cam = commit --amend -m
    cl = clone
    cm = commit -m
    co = checkout
    cob = checkout -b
    cod = checkout develop
    com = checkout master
    cp = cherry-pick
    df = diff
  	g = grep --break --heading --line-number
    lg = log --pretty=oneline
    map = log --graph --full-history --all --color --pretty=format:"%x1b[31m%h%x09%x1b[32m%d%x1b[0m%x20%s"
    mt = mergetool
    pl = pull
    ps = push
    rb = rebase
    rba = rebase --abort
    rbc = rebase --continue
    rbim = rebase -i master
    r = reset
    rh = reset --hard
    rs = reset --soft
    rqp = request-pull
    rqpm = request-pull -m
    sb = show-branch
    s = stash
    sa = stash apply
    sl = stash list
    spu = stash push -u
    st = status
    t = tag
[core]
    editor = vim
[merge]
    tool = vim
[diff]
    tool = vim
[grep]
  	lineNumber = true
	  extendRegexp = true
[push]
	  followTags = true
[mergetool]
    keepBackup = false
